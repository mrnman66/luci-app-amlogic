name: Build Luci App Amlogic Package

on:
  workflow_dispatch: # Возможность ручного запуска

env:
  OPENWRT_VERSION: 24.10.2
  PLATFORM_TARGET: armsr/armv8 # Определяем полную цель, включая "/armv8"
  ARCH_NAME: armsr-armv8 # Только имя архитектуры без "/"

jobs:
  build_and_publish:
    runs-on: ubuntu-latest

    steps:
      - name: 📂 Check out source code
        uses: actions/checkout@v3

      - name: ⚙️ Download and extract OpenWRT SDK
        run: |
          sudo apt-get update && sudo apt-get install -y curl unzip make gcc binutils patch diffstat git-core python3 libncurses-dev zlib1g-dev gawk chrpath xz-utils rsync bison flex bc pkg-config zstd # добавляем zstd для распаковки архива
          curl -fsSL https://downloads.openwrt.org/releases/${OPENWRT_VERSION}/targets/${PLATFORM_TARGET}/openwrt-sdk-${OPENWRT_VERSION}-${ARCH_NAME}_gcc-13.3.0_musl.Linux-x86_64.tar.zst -O
          tar --use-compress-program=zstd -xf openwrt-sdk-*.tar.zst
          
          # Находим главный каталог SDK
          SDK_DIR="$(ls -F | grep '/$' | grep -v '^luci-app-amlogic/' | head -n 1)" || { echo "Failed to find SDK directory"; exit 1; }
          cd "$SDK_DIR" || { echo "Failed to change directory"; exit 1; }

          # Проверяем наличие необходимой структуры
          ls -la || { echo "Failed to list contents of SDK directory"; exit 1; }
          test -d scripts && test -x scripts/feeds || { echo "Directory 'scripts' or script 'feeds' is missing"; exit 1; }

          scripts/feeds update -a && scripts/feeds install -a

      - name: 🛠️ Copy sources into SDK structure
        run: |
          cd "${SDK_DIR}" || { echo "Failed to change directory"; exit 1; }
          mkdir -p feeds/custom || { echo "Failed to create custom feed directory"; exit 1; }
          # Выполняем копирование конкретных файлов и папок
          cp -R ../../luci-app-amlogic/{Makefile,root,src,po,LICENSE} feeds/custom/
          scripts/feeds install -p custom -f luci-app-amlogic

      - name: ✨ Start compilation process
        run: |
          cd "${SDK_DIR}" || { echo "Failed to change directory"; exit 1; }
          make defconfig
          make package/luci-app-amlogic/compile V=s

      - name: 🖥️ Create a new release in your repository
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
        with:
          tag_name: "v${OPENWRT_VERSION}-${{ github.run_id }}"
          release_name: "OpenWRT Packages (${OPENWRT_VERSION})"
          draft: false
          prerelease: true

      - name: 🗃️ Upload built IPK file(s) as release assets
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
        with:
          upload_url: ${{steps.create_release.outputs.upload_url}} # Получаем URL выгрузки из шага выше
          asset_path: openwrt-*/bin/packages/*/*.ipk
          asset_name: luci-app-amlogic.ipk
          asset_content_type: application/octet-stream
